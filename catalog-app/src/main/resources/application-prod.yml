spring:
  application:
    name: catalog_database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/catalog_database
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:password}